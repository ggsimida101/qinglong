name: build static

on:
  push:
    branches: [ v2.2.0 ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '14'
      
      - name: build front and back
        run: |
          yarn install
          yarn build
          yarn build-back
        
      - name: copy to static repo
        env: 
          # GITHUB_REPO: gitee.com/ggsimida101/qinglong-static
          GITHUB_REPO: github.com/ggsimida101/qinglong-static
        run: |
          mkdir -p static
          cd ./static 
          cp -rf ../dist ./ && cp -rf ../build ./
          git init && git add .
          git config user.name "ggsimida101"
          git config user.email "imggsimida101@gmail.com"
          git commit --allow-empty -m "copy static at $(date +'%Y-%m-%d %H:%M:%S')"
          git push --force --quiet "https://ggsimida101:${{ secrets.API_TOKEN }}@${GITHUB_REPO}.git" master:v2.2.0